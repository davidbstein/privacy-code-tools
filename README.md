# privacy-code-tools
Some helper tools to avoid using a spreadsheet

## Setup instructions

you need to install:
  - `fish` shell
  - `npm`
  - `aws` and `eb` cli tools.

run:
  - `eb init`
  - `npm install`

to deploy new code:
  - `deploy.fish`
